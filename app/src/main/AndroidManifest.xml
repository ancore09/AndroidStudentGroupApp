<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studentappmvvm">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NightBlue">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.AppActivity" android:theme="@style/Theme.NightBlue"/>
        <activity android:name=".ui.LoginActivity" android:theme="@style/Theme.NightBlue"/>
        <activity android:name=".ui.SignupActivity" android:theme="@style/Theme.NightBlue"/>
        <activity android:name=".ui.TeacherAppActivity" android:theme="@style/Theme.NightBlue" />
    </application>

</manifest>